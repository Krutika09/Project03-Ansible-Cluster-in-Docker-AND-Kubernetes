# ansible-master.yaml
apiVersion: v1
kind: Pod
metadata:
  name: ansible-master
  labels:
    role: master
spec:
  containers:
  - name: master
    image: krutika09/rhel-ansible:v1
    ports:
    - containerPort: 22
